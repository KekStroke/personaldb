About Unsupervised Learning .
. Type of [Machine Learning# Model].
. Data is not labeled.
. Model tries to identify patterns without external help
  (based only on characteristics of the data itself).

. Examples:
  * [Clustering].
  * [Anomaly Detection].